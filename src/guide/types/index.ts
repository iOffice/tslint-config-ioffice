import * as complex from './complex';
import * as primitives from './primitives';

const name = 'Types';
const reference = 'types';
const order: any[] = [
  primitives,
  complex,
];

export {
  name,
  reference,
  order,
};
