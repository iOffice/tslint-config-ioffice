import * as leading from './leading';
import * as dangling from './trailing';

const name = 'Commas';
const reference = 'commas';
const order: any[] = [
  leading,
  dangling,
];

export {
  name,
  reference,
  order,
};
