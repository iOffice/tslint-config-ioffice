import * as braces from './braces';
import * as cuddledElses from './cuddled-elses';

const name = 'Blocks';
const reference = 'blocks';
const order: any[] = [
  braces,
  cuddledElses,
];

export {
  name,
  reference,
  order,
};
