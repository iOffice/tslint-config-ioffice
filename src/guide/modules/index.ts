import * as useThem from './use-them';
import * as singleExport from './single-export';

const name = 'Modules';
const reference = 'modules';
const order: any[] = [
  useThem,
  singleExport,
];

export {
  name,
  reference,
  order,
};
