import * as unusedParameters from './unused-parameters';

const name = 'Functions';
const reference = 'functions';
const order: any[] = [
  unusedParameters,
];

export {
  name,
  reference,
  order,
};
